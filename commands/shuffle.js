const { MessageEmbed } = require("discord.js")

module.exports = {
    name: 'shuffle',
    description: 'shuffluje kolejke',
    usage: '-shuffle',
    aliases: [],
    args: false,
    execute(player, message, args) {

    } 
}