const { MessageEmbed } = require("discord.js")

module.exports = {
    name: 'loop',
    description: 'Włącza bądź wyłącza loopa',
    usage: '-loop [song|queue|off]',
    aliases: [],
    args: true,
    execute(player, message, args) {} 
}