const { MessageEmbed } = require("discord.js")

module.exports = {
    name: 'remove',
    description: 'Usuwa piosenke z kolejki',
    usage: '-remove <nr piosenki>',
    aliases: ['r'],
    args: true,
    execute(player, message, args) {
    } 
}