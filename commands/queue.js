const { MessageEmbed } = require('discord.js');

module.exports = {
    name: 'queue',
    description: 'Wyświetla kolejke',
    usage: '-queue',
    aliases: ['q'],
    args: false,
    async execute(player, message, args) {} 
}